#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    for (int i=0; i<t; i++) {
        int n, a, b, c;
        cin >> n >> a >> b >> c;
        int total=0;
        if (c>=n) {
            cout << 0 << " " << 0 << " " << n;
        }
    }
}
